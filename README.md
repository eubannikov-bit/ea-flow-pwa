# ea.flow PWA

Browser installable PWA for family finance tracking.

Open the GitHub Pages URL on iPhone in Safari, then use:

`Share` -> `Add to Home Screen`

The Google Apps Script URL is stored only in the browser local storage after it is entered in the app settings.
